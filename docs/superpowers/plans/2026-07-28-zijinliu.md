# zijinliu (行业资金流排行榜) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建一个复盘观察用的行业板块资金流排行榜 skill——抓东财行业板块今日主力资金净流入，按"额+占比"双口径排流入/流出两端榜单，存 JSON + 打印 markdown 摘要。

**Architecture:** 镜像 renqibang 分层（`main.py` 编排 + `screener/{fetcher,storage}.py` + `probe_fs.py` + `tests/`）。fetcher 走东财 `push2delay` clist 接口（已实测可用，走系统代理 `trust_env=True`），降序/升序各取一页覆盖流入/流出两端，端内按 `(f62,f184)` 去重消除同行业多层级 BK 重叠。仅 requests 依赖，无 Playwright。

**Tech Stack:** Python 3.10+、requests、pytest。东财 `push2delay.eastmoney.com/api/qt/clist/get`。

**Spec:** [docs/superpowers/specs/2026-07-28-zijinliu-design.md](../specs/2026-07-28-zijinliu-design.md)

**关键参考实现：** [.claude/skills/renqibang/screener/fetcher.py](../../../.claude/skills/renqibang/screener/fetcher.py)（代理/重试/字段补全模式）、[.claude/skills/renqibang/main.py](../../../.claude/skills/renqibang/main.py)（编排+摘要打印）、[.claude/skills/renqibang/screener/storage.py](../../../.claude/skills/renqibang/screener/storage.py)（JSON 读写）。

---

## File Structure

| 文件 | 职责 |
|---|---|
| `.claude/skills/zijinliu/requirements.txt` | 仅 `requests` |
| `.claude/skills/zijinliu/.gitignore` | 忽略 `data/*.json` / `__pycache__` / `.pytest_cache`，保留 `.gitkeep` |
| `.claude/skills/zijinliu/data/.gitkeep` | 占位，让 data/ 进版本库 |
| `.claude/skills/zijinliu/screener/__init__.py` | 包标识（空） |
| `.claude/skills/zijinliu/screener/fetcher.py` | push2delay clist 会话 + `parse` + `dedup` + `fetch_top_flows` |
| `.claude/skills/zijinliu/screener/storage.py` | `zijin_<date>.json` 读写 |
| `.claude/skills/zijinliu/probe_fs.py` | 开发/维护用：探接口、验证字段与去重策略 |
| `.claude/skills/zijinliu/main.py` | 编排入口：CHECKPOINT → fetch → save → 打印摘要 |
| `.claude/skills/zijinliu/SKILL.md` | 技能描述、执行步骤、边界、反例 |
| `.claude/skills/zijinliu/tests/__init__.py` | 包标识（空） |
| `.claude/skills/zijinliu/tests/test_fetcher.py` | parse / dedup / fetch_top_flows 测试 |
| `.claude/skills/zijinliu/tests/test_storage.py` | save/load round-trip |
| `.claude/skills/zijinliu/tests/test_main.py` | 编排 + CHECKPOINT + 摘要 |
| `.claude/skills/zijinliu/test-prompts.json` | 触发词测试用例 |

---

### Task 1: 脚手架（目录 + 依赖 + 包标识）

**Files:**
- Create: `.claude/skills/zijinliu/requirements.txt`
- Create: `.claude/skills/zijinliu/.gitignore`
- Create: `.claude/skills/zijinliu/data/.gitkeep`
- Create: `.claude/skills/zijinliu/screener/__init__.py`
- Create: `.claude/skills/zijinliu/tests/__init__.py`

- [ ] **Step 1: 创建目录与文件**

```bash
cd c:/project/fox/xuan-gu-qi
mkdir -p .claude/skills/zijinliu/screener .claude/skills/zijinliu/tests .claude/skills/zijinliu/data
```

`.claude/skills/zijinliu/requirements.txt`：
```
requests
```

`.claude/skills/zijinliu/.gitignore`：
```
data/*.json
!data/.gitkeep
__pycache__/
.pytest_cache/
```

`.claude/skills/zijinliu/data/.gitkeep`：（空文件）

`.claude/skills/zijinliu/screener/__init__.py`：（空文件）

`.claude/skills/zijinliu/tests/__init__.py`：（空文件）

- [ ] **Step 2: 验证目录结构**

Run: `ls -R .claude/skills/zijinliu`
Expected: 看到 `data/ .gitignore requirements.txt screener/ tests/`，`screener/__init.py__`、`tests/__init__.py` 存在。

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/zijinliu/
git commit -m "chore(zijinliu): 脚手架(目录/依赖/包标识)"
```

---

### Task 2: probe_fs.py（验证接口 + 去重策略）

开发/维护用脚本。先跑它确认东财接口当前可用、字段语义正确、去重策略有效，再写 fetcher。

**Files:**
- Create: `.claude/skills/zijinliu/probe_fs.py`

- [ ] **Step 1: 写 probe 脚本**

`.claude/skills/zijinliu/probe_fs.py`：
```python
"""探测东财行业板块资金流接口 — 开发/维护用。

确认: ① 接口可用(push2delay + 系统代理) ② 字段语义 ③ 两端取数 ④ 去重策略有效。
接口改版/被封时重跑此脚本诊断。非测试覆盖部分,仅供人工核查。
"""
import re
import requests

_S = requests.Session()
_S.trust_env = True   # 跟随系统代理(同 renqibang), push2delay 直连会被关
_S.headers.update({
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
                  "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
    "Referer": "https://data.eastmoney.com/",
})

URL = "http://push2delay.eastmoney.com/api/qt/clist/get"
FS = "m:90+t:2+f:!50"
FIELDS = "f12,f14,f3,f62,f184,f66,f72"
_SUF = re.compile(r"[ⅡⅢⅣⅤⅥ]")


def fetch(po: int, pz: int = 100) -> list:
    params = {"pn": "1", "pz": str(pz), "po": str(po),
              "fid": "f62", "fs": FS, "fields": FIELDS, "fltt": "2"}
    r = _S.get(URL, params=params, timeout=15)
    data = (r.json() or {}).get("data") or {}
    diff = data.get("diff") or []
    items = list(diff.values()) if isinstance(diff, dict) else list(diff)
    print(f"  po={po} pz={pz} → status={r.status_code} total={data.get('total')} rows={len(items)}")
    return items


def dedup(items):
    """端内按 (f62,f184) 去重, 保留无罗马后缀名者。"""
    best = {}
    for it in items:
        if it.get("f62") is None:
            continue
        key = (it["f62"], it.get("f184"))
        cur = best.get(key)
        if cur is None or (_SUF.search(cur.get("f14", "")) and not _SUF.search(it.get("f14", ""))):
            best[key] = it
    return list(best.values())


if __name__ == "__main__":
    print("=== 流入端 (po=1 降序) ===")
    inflow = fetch(po=1)
    print("=== 流出端 (po=0 升序) ===")
    outflow = fetch(po=0)

    print("\n=== 流入端去重前/后 ===", len(inflow), "→", len(dedup(inflow)))
    print("Top 8 流入:")
    for it in dedup(inflow)[:8]:
        print(f"  {it.get('f14'):<12} f3={it.get('f3'):>6}%  f62={it.get('f62',0)/1e8:>7.2f}亿  f184={it.get('f184')}%  [{it.get('f12')}]")

    print("\n=== 流出端去重前/后 ===", len(outflow), "→", len(dedup(outflow)))
    print("Top 8 流出:")
    for it in dedup(outflow)[:8]:
        print(f"  {it.get('f14'):<12} f3={it.get('f3'):>6}%  f62={it.get('f62',0)/1e8:>7.2f}亿  f184={it.get('f184')}%  [{it.get('f12')}]")

    print("\n✅ 若两端都有合理数据(流入为正/流出为负, 行业名正常), 接口与去重策略可用。")
```

- [ ] **Step 2: 运行 probe 验证**

Run: `cd .claude/skills/zijinliu && python probe_fs.py`
Expected: 流入端 Top 出现正净流入行业（如银行/食品饮料/电力），流出端 Top 出现负净流出行业（如电子/半导体），去重后条数 ≤ 去重前，行业名无乱码（若 Windows 终端乱码，在脚本顶部加 `import sys; sys.stdout.reconfigure(encoding='utf-8')` 后重跑）。

> 若接口返回空/超时：先确认系统代理(Clash)开启；仍失败则东财接口可能被封，暂停后续 Task，参考 memory `eastmoney-push2-mirror-availability`。

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/zijinliu/probe_fs.py
git commit -m "chore(zijinliu): probe_fs 接口/去重探测脚本"
```

---

### Task 3: fetcher.parse（单条字段解析，TDD）

**Files:**
- Create: `.claude/skills/zijinliu/screener/fetcher.py`
- Test: `.claude/skills/zijinliu/tests/test_fetcher.py`

- [ ] **Step 1: 写失败测试**

`.claude/skills/zijinliu/tests/test_fetcher.py`：
```python
"""fetcher 测试: parse / dedup / fetch_top_flows。"""
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from screener import fetcher


def test_parse_maps_all_fields():
    raw = {"f12": "BK1283", "f14": "银行", "f3": 1.04,
           "f62": 1527127040, "f184": 4.3, "f66": 1108062464, "f72": 419064576}
    r = fetcher.parse(raw)
    assert r["code"] == "BK1283"
    assert r["name"] == "银行"
    assert r["change_pct"] == 1.04
    assert r["main_net"] == 1527127040
    assert r["main_net_yi"] == 15.27          # ÷1e8 保留两位
    assert r["main_pct"] == 4.3
    assert r["super_large_net"] == 1108062464
    assert r["large_net"] == 419064576


def test_parse_negative_flow():
    raw = {"f12": "BK0xxx", "f14": "电子", "f3": -4.23,
           "f62": -559410000, "f184": -8.64, "f66": -300000000, "f72": -150000000}
    r = fetcher.parse(raw)
    assert r["main_net_yi"] == -5.59
    assert r["change_pct"] == -4.23
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'screener.fetcher'`（或 parse 未定义）。

- [ ] **Step 3: 写 fetcher.py（会话配置 + parse）**

`.claude/skills/zijinliu/screener/fetcher.py`：
```python
"""数据获取层 — 东财行业板块资金流(push2delay clist)。

网络要点(同 renqibang/screener/fetcher.py):
  push2delay 直连(trust_env=False)会被服务端关连接; 必须跟随系统代理
  (trust_env=True), 用 http 非 https, 带浏览器 UA + Referer。
  偶发空响应, 带 RETRIES 退避重试。
"""
import re
import time

import requests

_session = requests.Session()
_session.trust_env = True
_session.headers.update({
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
                  "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36",
    "Referer": "https://data.eastmoney.com/",
})

CLIST_URL = "http://push2delay.eastmoney.com/api/qt/clist/get"
FS = "m:90+t:2+f:!50"
FIELDS = "f12,f14,f3,f62,f184,f66,f72"
RETRIES = 3
_SUF = re.compile(r"[ⅡⅢⅣⅤⅥ]")


def parse(row: dict) -> dict:
    """单条原始 dict → 统一结构(含 main_net_yi 亿元换算)。"""
    return {
        "code": row.get("f12", ""),
        "name": row.get("f14", ""),
        "change_pct": row.get("f3"),
        "main_net": row.get("f62"),
        "main_net_yi": round((row.get("f62") or 0) / 1e8, 2),
        "main_pct": row.get("f184"),
        "super_large_net": row.get("f66"),
        "large_net": row.get("f72"),
    }
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: PASS（2 passed）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/screener/fetcher.py .claude/skills/zijinliu/tests/test_fetcher.py
git commit -m "feat(zijinliu): fetcher.parse 单条字段解析(含亿元换算)"
```

---

### Task 4: fetcher.dedup（端内去重，TDD）

**Files:**
- Modify: `.claude/skills/zijinliu/screener/fetcher.py`（追加 `dedup`）
- Test: `.claude/skills/zijinliu/tests/test_fetcher.py`（追加用例）

- [ ] **Step 1: 追加失败测试**

在 `tests/test_fetcher.py` 末尾追加：
```python
def test_dedup_merges_same_flow_keeps_no_suffix():
    """银行(一级) 与 银行Ⅱ(二级) f62/f184 相同 → 合并为一个, 保留无后缀的'银行'。"""
    items = [
        {"name": "银行Ⅱ", "main_net": 1527127040, "main_pct": 4.3},
        {"name": "银行",   "main_net": 1527127040, "main_pct": 4.3},
    ]
    out = fetcher.dedup(items)
    assert len(out) == 1
    assert out[0]["name"] == "银行"


def test_dedup_keeps_different_flows():
    """通信(-212) 与 通信设备(-209) f62 不同 → 都保留。"""
    items = [
        {"name": "通信",     "main_net": -21205000000, "main_pct": -11.24},
        {"name": "通信设备", "main_net": -20909000000, "main_pct": -11.89},
    ]
    out = fetcher.dedup(items)
    assert len(out) == 2


def test_dedup_skips_none_main_net():
    items = [
        {"name": "停牌行业", "main_net": None, "main_pct": None},
        {"name": "银行", "main_net": 1000, "main_pct": 5},
    ]
    out = fetcher.dedup(items)
    assert len(out) == 1
    assert out[0]["name"] == "银行"
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: FAIL — `AttributeError: module 'screener.fetcher' has no attribute 'dedup'`。

- [ ] **Step 3: 在 fetcher.py 追加 dedup**

在 `screener/fetcher.py` 的 `parse` 函数之后追加：
```python
def dedup(industries: list) -> list:
    """端内去重: 同行业多层级 BK 的 (main_net, main_pct) 相同, 合并为一个。

    同值多条中优先保留行业名不含罗马后缀(ⅡⅢⅣⅤⅥ)者(即一级名)。
    main_net 为 None(停牌/缺字段)的条目跳过。
    """
    best = {}
    for it in industries:
        if it.get("main_net") is None:
            continue
        key = (it["main_net"], it.get("main_pct"))
        cur = best.get(key)
        if cur is None or (_SUF.search(cur.get("name", "")) and not _SUF.search(it.get("name", ""))):
            best[key] = it
    return list(best.values())
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: PASS（5 passed）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/screener/fetcher.py .claude/skills/zijinliu/tests/test_fetcher.py
git commit -m "feat(zijinliu): fetcher.dedup 端内去重(同行业多层级BK合并)"
```

---

### Task 5: fetcher.fetch_top_flows（两端请求，TDD mock）

**Files:**
- Modify: `.claude/skills/zijinliu/screener/fetcher.py`（追加 `_request` / `_diff_list` / `fetch_top_flows`）
- Test: `.claude/skills/zijinliu/tests/test_fetcher.py`（追加用例）

- [ ] **Step 1: 追加失败测试**

在 `tests/test_fetcher.py` 末尾追加：
```python
def test_fetch_top_flows_calls_both_orders(monkeypatch):
    """应发 po=1(降序/流入) + po=0(升序/流出) 两次请求, 返回 inflow/outflow。"""
    calls = []

    def fake_request(po, pz):
        calls.append((po, pz))
        if po == 1:
            return {"data": {"diff": {"1": {"f12": "BK1", "f14": "银行",
                    "f3": 1.0, "f62": 1000, "f184": 5, "f66": 500, "f72": 300}}}}
        return {"data": {"diff": {"1": {"f12": "BK2", "f14": "电子",
                "f3": -2.0, "f62": -2000, "f184": -8, "f66": -1000, "f72": -500}}}}

    monkeypatch.setattr(fetcher, "_request", fake_request)
    r = fetcher.fetch_top_flows(per_end=50)
    assert calls == [(1, 50), (0, 50)]
    assert len(r["inflow"]) == 1 and r["inflow"][0]["name"] == "银行"
    assert len(r["outflow"]) == 1 and r["outflow"][0]["name"] == "电子"
    assert r["inflow"][0]["main_net_yi"] == 0.0   # 1000/1e8


def test_fetch_top_flows_dedup_applied(monkeypatch):
    """两端各自做端内去重。"""
    def fake_request(po, pz):
        if po == 1:
            return {"data": {"diff": [
                {"f12": "BK1", "f14": "银行", "f62": 1000, "f184": 5},
                {"f12": "BK2", "f14": "银行Ⅱ", "f62": 1000, "f184": 5},
            ]}}
        return {"data": {"diff": []}}
    monkeypatch.setattr(fetcher, "_request", fake_request)
    r = fetcher.fetch_top_flows(per_end=100)
    assert len(r["inflow"]) == 1          # 银行/银行Ⅱ 合并
    assert len(r["outflow"]) == 0
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: FAIL — `AttributeError: module 'screener.fetcher' has no attribute '_request'`（或 fetch_top_flows）。

- [ ] **Step 3: 在 fetcher.py 追加请求函数**

在 `screener/fetcher.py` 的 `dedup` 之后追加：
```python
def _request(po: int, pz: int) -> dict:
    """请求 clist, 返回原始 json(失败/重试耗尽返回 {})。可被测试 mock。"""
    params = {"pn": "1", "pz": str(pz), "po": str(po),
              "fid": "f62", "fs": FS, "fields": FIELDS, "fltt": "2"}
    for attempt in range(RETRIES):
        try:
            r = _session.get(CLIST_URL, params=params, timeout=15)
            return r.json() or {}
        except Exception as e:
            if attempt < RETRIES - 1:
                time.sleep(0.5 * (attempt + 1))
                continue
            print(f"push2delay 请求失败(po={po}): {e}", flush=True)
            return {}


def _diff_list(payload: dict) -> list:
    """从 clist 响应取 diff 列表(接口返回 dict, 按 .values() 取)。"""
    data = payload.get("data") or {}
    diff = data.get("diff") or []
    if isinstance(diff, dict):
        return list(diff.values())
    return list(diff)


def fetch_top_flows(per_end: int = 100) -> dict:
    """两端各取 Top per_end: po=1 降序(流入端) + po=0 升序(流出端)。

    各端 parse + dedup 后返回 {"inflow": [...], "outflow": [...]}。
    """
    inflow = dedup([parse(r) for r in _diff_list(_request(po=1, pz=per_end))])
    outflow = dedup([parse(r) for r in _diff_list(_request(po=0, pz=per_end))])
    return {"inflow": inflow, "outflow": outflow}
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_fetcher.py -v`
Expected: PASS（7 passed）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/screener/fetcher.py .claude/skills/zijinliu/tests/test_fetcher.py
git commit -m "feat(zijinliu): fetcher.fetch_top_flows 两端取数(降序+升序)"
```

---

### Task 6: storage.py（JSON 读写，TDD）

**Files:**
- Create: `.claude/skills/zijinliu/screener/storage.py`
- Test: `.claude/skills/zijinliu/tests/test_storage.py`

- [ ] **Step 1: 写失败测试**

`.claude/skills/zijinliu/tests/test_storage.py`：
```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from screener import storage


def test_save_load_roundtrip(tmp_path):
    inflow = [{"code": "BK1", "name": "银行", "main_net_yi": 15.27}]
    outflow = [{"code": "BK2", "name": "电子", "main_net_yi": -5.59}]
    path = storage.save_results("2026-07-28", inflow, outflow, str(tmp_path))
    assert os.path.exists(path)
    assert path.endswith("zijin_2026-07-28.json")

    data = storage.load_results("2026-07-28", str(tmp_path))
    assert data["date"] == "2026-07-28"
    assert data["inflow_count"] == 1
    assert data["outflow_count"] == 1
    assert data["count"] == 2
    assert data["inflow"][0]["name"] == "银行"
    assert data["outflow"][0]["name"] == "电子"
    assert data["source"] == "东方财富行业板块资金流(push2delay clist)"


def test_load_missing_returns_none(tmp_path):
    assert storage.load_results("2099-01-01", str(tmp_path)) is None


def test_save_overwrites(tmp_path):
    storage.save_results("2026-07-28", [{"name": "旧"}], [], str(tmp_path))
    storage.save_results("2026-07-28", [{"name": "新"}], [], str(tmp_path))
    data = storage.load_results("2026-07-28", str(tmp_path))
    assert data["inflow"][0]["name"] == "新"
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_storage.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'screener.storage'`。

- [ ] **Step 3: 写 storage.py**

`.claude/skills/zijinliu/screener/storage.py`：
```python
"""zijin_<date>.json 读写。

文件名 zijin_<date>.json, 结构与 spec 一致。save 覆盖写(防覆盖由 main 的
CHECKPOINT 交互处理)。
"""
import datetime
import json
import os


def _now_iso() -> str:
    return datetime.datetime.now().strftime("%Y-%m-%dT%H:%M:%S")


def save_results(date_str: str, inflow: list, outflow: list,
                 output_dir: str) -> str:
    os.makedirs(output_dir, exist_ok=True)
    result = {
        "date": date_str,
        "fetched_at": _now_iso(),
        "source": "东方财富行业板块资金流(push2delay clist)",
        "sort": "流入端 po=1 降序 / 流出端 po=0 升序（各端已去重）",
        "inflow_count": len(inflow),
        "outflow_count": len(outflow),
        "count": len(inflow) + len(outflow),
        "inflow": inflow,
        "outflow": outflow,
    }
    path = os.path.join(output_dir, f"zijin_{date_str}.json")
    with open(path, "w", encoding="utf-8") as f:
        json.dump(result, f, ensure_ascii=False, indent=2)
    return path


def load_results(date_str: str, output_dir: str):
    path = os.path.join(output_dir, f"zijin_{date_str}.json")
    if not os.path.exists(path):
        return None
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_storage.py -v`
Expected: PASS（3 passed）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/screener/storage.py .claude/skills/zijinliu/tests/test_storage.py
git commit -m "feat(zijinliu): storage zijin_<date>.json 读写"
```

---

### Task 7: main.py（编排 + CHECKPOINT + 摘要，TDD）

**Files:**
- Create: `.claude/skills/zijinliu/main.py`
- Test: `.claude/skills/zijinliu/tests/test_main.py`

- [ ] **Step 1: 写失败测试**

`.claude/skills/zijinliu/tests/test_main.py`：
```python
import sys, os
sys.path.insert(0, os.path.join(os.path.dirname(__file__), ".."))

from screener import storage
import main


class _FakeFetcher:
    @staticmethod
    def fetch_top_flows(per_end=100):
        return {
            "inflow": [{"code": "BK1", "name": "银行", "change_pct": 1.04,
                        "main_net_yi": 15.27, "main_pct": 4.3,
                        "super_large_net": 1108062464, "main_net": 1527127040,
                        "large_net": 419064576}],
            "outflow": [{"code": "BK2", "name": "电子", "change_pct": -4.23,
                         "main_net_yi": -5.59, "main_pct": -8.64,
                         "super_large_net": -1000, "main_net": -559410000,
                         "large_net": -500}],
        }


def test_run_creates_snapshot_and_prints(tmp_path, capsys):
    ok = main.run(today_str="2026-07-28", output_dir=str(tmp_path),
                  top=20, outflow_top=10, fetcher=_FakeFetcher)
    assert ok is True
    assert os.path.exists(os.path.join(str(tmp_path), "zijin_2026-07-28.json"))
    out = capsys.readouterr().out
    assert "银行" in out
    assert "电子" in out
    assert "主力净流入" in out and "主力净流出" in out


def test_run_checkpoint_blocks_without_force(tmp_path, capsys):
    # 预置一个已存在的快照
    open(os.path.join(str(tmp_path), "zijin_2026-07-28.json"), "w").close()
    ok = main.run(today_str="2026-07-28", output_dir=str(tmp_path),
                  fetcher=_FakeFetcher)
    assert ok is False
    assert "已存在" in capsys.readouterr().out


def test_run_force_overwrites(tmp_path):
    open(os.path.join(str(tmp_path), "zijin_2026-07-28.json"), "w").close()
    ok = main.run(today_str="2026-07-28", output_dir=str(tmp_path),
                  force=True, fetcher=_FakeFetcher)
    assert ok is True
    data = storage.load_results("2026-07-28", str(tmp_path))
    assert data["inflow"][0]["name"] == "银行"
```

- [ ] **Step 2: 运行测试，确认失败**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/test_main.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'main'`。

- [ ] **Step 3: 写 main.py**

`.claude/skills/zijinliu/main.py`：
```python
"""行业资金流排行榜 — 编排入口。

流程: CHECKPOINT(防覆盖) → fetch_top_flows(两端) → 各端已 parse+dedup →
     存 zijin_<date>.json → 终端打印 markdown 摘要。

可注入 fetcher(测试用); 默认用 screener.fetcher。
"""
import argparse
import datetime
import os
import sys

sys.path.insert(0, os.path.dirname(__file__))

from screener import fetcher as _default_fetcher
from screener import storage


def _yi(val) -> str:
    """超大单等原始元值 → 亿元字符串(无值返回 '-')。"""
    if val is None:
        return "-"
    return f"{round(val / 1e8, 2)}"


def _print_summary(data: dict, top: int, outflow_top: int) -> None:
    date_str = data["date"]
    print(f"\n# 行业资金流 · {date_str}（东方财富·今日）")
    print(f"流入 {data['inflow_count']} 个 / 流出 {data['outflow_count']} 个 | "
          "主力净流入额降序（占比 = 净流入/成交额，排除体量偏差）\n")

    print(f"## 主力净流入 Top {top}")
    print("| # | 行业 | 涨跌% | 主力净流入(亿) | 占比% | 超大单(亿) |")
    print("|---|---|---|---|---|---|")
    for i, it in enumerate(data["inflow"][:top], 1):
        print(f"| {i} | {it.get('name', '')} | {it.get('change_pct', '-')} | "
              f"{it.get('main_net_yi', '-')} | {it.get('main_pct', '-')} | "
              f"{_yi(it.get('super_large_net'))} |")

    print(f"\n## 主力净流出 Top {outflow_top}")
    print("| # | 行业 | 涨跌% | 主力净流出(亿) | 占比% | 超大单(亿) |")
    print("|---|---|---|---|---|---|")
    for i, it in enumerate(data["outflow"][:outflow_top], 1):
        print(f"| {i} | {it.get('name', '')} | {it.get('change_pct', '-')} | "
              f"{it.get('main_net_yi', '-')} | {it.get('main_pct', '-')} | "
              f"{_yi(it.get('super_large_net'))} |")


def run(today_str: str | None = None, output_dir: str | None = None,
        top: int = 20, outflow_top: int = 10, per_end: int = 100,
        force: bool = False, fetcher=None) -> bool:
    today_str = today_str or datetime.date.today().strftime("%Y-%m-%d")
    base_dir = os.path.dirname(os.path.abspath(__file__))
    output_dir = output_dir or os.path.join(base_dir, "data")
    fetcher = fetcher or _default_fetcher

    # 1. CHECKPOINT: 今日快照已存在且未 --force → STOP
    path = os.path.join(output_dir, f"zijin_{today_str}.json")
    if os.path.exists(path) and not force:
        print(f"⚠️ 今日资金流快照已存在：{path}，加 --force 覆盖。", flush=True)
        return False

    # 2. 两端取数(已 parse + dedup)
    print(f"[{today_str}] 抓取东财行业板块资金流（两端各 Top {per_end}）...", flush=True)
    flows = fetcher.fetch_top_flows(per_end=per_end)
    print(f"流入端 {len(flows['inflow'])} 条 / 流出端 {len(flows['outflow'])} 条（去重后）",
          flush=True)

    # 3. 保存
    saved = storage.save_results(today_str, flows["inflow"], flows["outflow"], output_dir)
    print(f"已保存：{saved}", flush=True)

    # 4. 摘要
    _print_summary(storage.load_results(today_str, output_dir), top, outflow_top)
    return True


def main():
    p = argparse.ArgumentParser(description="行业资金流排行榜(复盘观察)")
    p.add_argument("--top", type=int, default=20)
    p.add_argument("--outflow-top", type=int, default=10)
    p.add_argument("--per-end", type=int, default=100)
    p.add_argument("--date", default=None)
    p.add_argument("--force", action="store_true")
    args = p.parse_args()
    ok = run(today_str=args.date, top=args.top, outflow_top=args.outflow_top,
             per_end=args.per_end, force=args.force)
    sys.exit(0 if ok else 1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 运行测试，确认通过**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/ -v`
Expected: PASS（全部测试通过：fetcher 7 + storage 3 + main 3 = 13）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/main.py .claude/skills/zijinliu/tests/test_main.py
git commit -m "feat(zijinliu): main 编排入口(CHECKPOINT+摘要)"
```

---

### Task 8: SKILL.md（技能描述与执行步骤）

**Files:**
- Create: `.claude/skills/zijinliu/SKILL.md`

- [ ] **Step 1: 写 SKILL.md**

`.claude/skills/zijinliu/SKILL.md`：
```markdown
---
name: zijinliu
description: 行业资金流排行榜 — 抓取东方财富行业板块当日主力资金净流入，按"净流入额+占比"双口径排出流入/流出两端榜单，存 JSON 并打印 markdown 摘要。复盘观察用，只看不动。当用户说"资金流"、"资金流入"、"主力资金"、"行业资金流"、"资金流向"、"主力净流入"、"zijinliu"时触发。
---

# 行业资金流排行榜（东财行业板块 · 今日）

抓取东方财富行业板块当日主力资金净流入快照，作为**复盘观察参考**。

> ⚠️ 资金流入 ≠ 买点。主力净流入反映当日资金方向，高位放量流入常是接力/出货。须结合 fupan 复盘与 qsht 选股体系的趋势/回踩/市值/业绩纪律使用，避免追涨。

## 使用方式

输入 `/zijinliu` 触发。

## 执行步骤

1. 🔴 CHECKPOINT（运行前检查今日快照是否已存在）:
   ```bash
   ls .claude/skills/zijinliu/data/zijin_$(date +%Y-%m-%d).json 2>/dev/null
   ```
   - 已存在 → 🛑 STOP，提示"今日资金流快照已抓取，重跑会覆盖"，询问确认；用户确认后加 `--force` 重跑。
   - 不存在 → 继续。

2. 确认依赖（首次）:
   ```bash
   cd .claude/skills/zijinliu && pip install -r requirements.txt
   ```

3. 运行:
   ```bash
   cd .claude/skills/zijinliu && python main.py
   ```
   > 仅 requests，无 Playwright，约 2–5 秒。可选参数：`--top 20 --outflow-top 10 --per-end 100 --date YYYY-MM-DD --force`。

4. 脚本会：抓东财行业板块资金流（流入端降序 + 流出端升序，各 Top100）→ 端内去重（消除银行/银行Ⅱ 同行业多层级重复）→ 存 `data/zijin_<date>.json` → 终端打印流入 Top20 + 流出 Top10。

5. 读取 `data/zijin_<date>.json` 或终端摘要向用户展示，并提醒"高流入 ≠ 买点"。

## 字段说明

| 字段 | 含义 |
|---|---|
| 行业 name | 东财行业板块名（已去重，保留一级名） |
| 涨跌% change_pct | 板块当日涨跌幅 |
| 主力净流入(亿) main_net_yi | 主力（超大单+大单）净流入额，÷1e8 转亿 |
| 占比% main_pct | 主力净流入 / 板块成交额，排除体量偏差（双口径之一） |
| 超大单(亿) super_large_net | 超大单净流入额 |

## 双口径解读

- **主力净流入额**（默认排序）：看绝对吸筹/出货量，偏向大体量行业。
- **净流入占比**：看相对进攻强度，排除体量偏差（小行业占比可能很高）。
- 两者结合：额大且占比高 = 资金实质性进场；额大但占比低 = 仅因体量大。

## 边界条件

| 触发 | 处理 |
|---|---|
| 非交易日 | 接口返回上一交易日数据，正常抓取，date 标注实际数据日 |
| push2delay 偶发空响应 | 3 轮退避重试 |
| 接口被封/改版 | 报错；重跑 `python probe_fs.py` 诊断字段/fs 是否变化 |
| 今日快照已存在 | CHECKPOINT 询问，`--force` 覆盖 |
| 盘中运行 | 资金流为盘中实时值（会随盘变化），复盘建议收盘后跑 |

## ❌ 不要做

- 不要把主力净流入当买入信号（高流入常是高位接力/出货窗口）
- 不要追多时间窗口/概念板块/个股（本 skill 仅行业·今日·双口径）
- 不要为资金流做 fallback 数据源（各源"主力"口径不一，混用易乱）

## 局限

- 资金流数据来自东财 Level-1 估算（超大单/大单按成交单金额切分），非真实逐笔，属估算值
- 仅今日快照，不反映多日持续性（持续流入须对比历史快照）
- "主力"定义按东财口径，与其他平台不可直接横向比较
```

- [ ] **Step 2: Commit**

```bash
git add .claude/skills/zijinliu/SKILL.md
git commit -m "docs(zijinliu): SKILL.md 技能描述与执行步骤"
```

---

### Task 9: 集成验证（真实接口 + test-prompts）

**Files:**
- Create: `.claude/skills/zijinliu/test-prompts.json`

- [ ] **Step 1: 写 test-prompts.json**

`.claude/skills/zijinliu/test-prompts.json`：
```json
[
  {"prompt": "资金流", "should_trigger": true},
  {"prompt": "资金流入排行榜", "should_trigger": true},
  {"prompt": "主力资金流向", "should_trigger": true},
  {"prompt": "行业资金流", "should_trigger": true},
  {"prompt": "zijinliu", "should_trigger": true},
  {"prompt": "今天主力净流入哪些行业", "should_trigger": true},
  {"prompt": "帮我选只能涨的股", "should_trigger": false},
  {"prompt": "人气榜", "should_trigger": false},
  {"prompt": "缩量回踩", "should_trigger": false}
]
```

- [ ] **Step 2: 真实接口集成跑一次**

Run: `cd .claude/skills/zijinliu && python main.py`
Expected:
- 终端打印 `[YYYY-MM-DD] 抓取东财行业板块资金流...` → `流入端 N 条 / 流出端 M 条（去重后）` → `已保存：...zijin_<date>.json` → markdown 摘要（流入 Top20 + 流出 Top10）。
- 流入端 Top 出现正净流入行业，流出端 Top 出现负净流出行业，行业名正常无乱码。
- exit code 0。

> 若终端中文乱码：在 main.py 顶部临时加 `import sys; sys.stdout.reconfigure(encoding='utf-8')`（仅本机 Windows 终端显示问题，不影响 JSON 内容）。

- [ ] **Step 3: 验证生成的 JSON**

Run: `cd .claude/skills/zijinliu && python -c "import json; d=json.load(open('data/zijin_'+__import__('datetime').date.today().strftime('%Y-%m-%d')+'.json',encoding='utf-8')); print('count',d['count'],'inflow_top',d['inflow'][0]['name'],d['inflow'][0]['main_net_yi'],'outflow_top',d['outflow'][0]['name'],d['outflow'][0]['main_net_yi'])"`
Expected: 打印 count、流入 Top1 名称+亿元、流出 Top1 名称+亿元，值合理。

- [ ] **Step 4: 全量测试回归**

Run: `cd .claude/skills/zijinliu && python -m pytest tests/ -v`
Expected: 全部 PASS（13 passed）。

- [ ] **Step 5: Commit**

```bash
git add .claude/skills/zijinliu/test-prompts.json .claude/skills/zijinliu/data/
git commit -m "test(zijinliu): 集成验证通过 + test-prompts 触发词用例"
```

> 注：`data/zijin_<date>.json` 含真实当日数据；若不希望进版本库，已在 `.gitignore` 忽略 `data/*.json`，则只 commit `test-prompts.json`。

---

## Self-Review（写完计划后自检）

**1. Spec coverage：**
- 定位/边界（只看不动、行业、今日、双口径）→ SKILL.md(Task 8) + main(Task 7) ✓
- 数据源 push2delay clist + 字段映射 → fetcher(Task 3-5) + probe(Task 2) ✓
- 两端取数 + 去重 → fetcher.fetch_top_flows(Task 5) + dedup(Task 4) ✓
- 文件结构 → Task 1 ✓
- JSON schema → storage(Task 6) ✓
- 编排/CHECKPOINT/摘要 → main(Task 7) ✓
- 边界条件 → SKILL.md(Task 8) + 重试(fetcher) ✓
- 测试 TDD → Task 3-7 ✓
- 不做项（YAGNI）→ 未实现个股/概念/多窗口/fallback ✓

**2. Placeholder scan：** 无 TBD/TODO；每个代码 step 均含完整可运行代码与命令。✓

**3. Type consistency：**
- `parse` 返回结构（code/name/change_pct/main_net/main_net_yi/main_pct/super_large_net/large_net）在 fetcher(Task 3)、storage 测试(Task 6)、main 摘要(Task 7)、test_main fake(Task 7) 中一致 ✓
- `fetch_top_flows` 返回 `{"inflow":[...],"outflow":[...]}`，main.run 与 test_fetcher 一致 ✓
- `dedup(industries) -> list`、`save_results(date_str, inflow, outflow, output_dir)` 签名在各 Task 间一致 ✓
- `run()` 返回 bool（CHECKPOINT 拦截返回 False），test_main 断言一致 ✓
