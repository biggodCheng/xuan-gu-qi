---
name: qsht-agent
description: 选股流水线 Agent — 编排创新高、涨停、缩量回踩、市值四步筛选，每步结果输出到 markdown 报告。当用户说"选股"、"筛选股票"、"qsht"、"缩量回踩选股"、"完整选股"、"跑一遍选股"时触发。
---

# qsht-agent — 选股流水线

## 概述

一键运行完整的选股流水线，自动编排 4 个筛选步骤：

1. **创新高**（chuangxingao）— 筛选当日收盘价创 100 个交易日新高的股票
2. **近期涨停**（zhangting）— 从创新高中筛选近15天有涨停的股票（主板>=9.5%, 科创板/创业板>=19.5%, 北交所>=29.5%）
3. **缩量回踩**（suolianghuicai）— 筛选涨停后缩量回踩的股票（策略1：连续缩量 + 价格回落）
4. **市值筛选**（shizhi）— 从缩量回踩股中筛出市值 < 200 亿的股票

每一步筛选出的股票都会输出到 markdown 报告中，方便逐步查看。

## 运行方式

```bash
python .claude/skills/qsht-agent/main.py
```

无需参数，所有参数使用默认值：
- 缩量回踩策略：策略 1（连续缩量 + 价格回落）
- shrink_ratio：0.8
- min_days：2
- 市值阈值：200 亿

## 输出

报告保存在 `.claude/skills/qsht-agent/output/{日期}.md`，包含：
- 筛选流水线统计（每步筛选数量）
- 第1步：创新高股票列表
- 第2步：近期涨停股票列表
- 第3步：缩量回踩股票列表
- 第4步：市值<200亿股票列表
- 最终结果汇总

运行完成后，使用 Read 工具读取报告文件并向用户展示完整内容。

## ⚠️ 注意事项与反例

### 本 skill 是编排器，不是新逻辑
本 skill **不实现任何筛选逻辑**，仅通过 subprocess 串联调用 4 个子 skill（chuangxingao → zhangting → suolianghuicai → shizhi）的 `main.py`。所有筛选能力来自子 skill，参数也由子 skill 的默认值承载，本 skill 不覆盖。

### ❌ 不要做（反例黑名单）

| 不要 | 原因 | 正确做法 |
|---|---|---|
| 期望本 skill 实现新的筛选条件 | 它只编排，无独立筛选逻辑 | 改对应的子 skill |
| 在非交易日运行 | 创新高步骤取不到行情，管道终止 | 工作日收盘后再跑 |
| 中途中断执行 | 4 步串行，打断会导致报告不完整 | 耐心等全部完成 |
| 自行修改子 skill 默认参数 | 默认值（策略1/0.8/2天/200亿）已调优 | 如需改，先理解对下游的影响 |

### 已知行为

- 任一步骤结果为 **0 只**时，管道**自动终止**并输出部分报告（该步及后续列表省略）
- 若当天提前终止，报告只含「筛选流水线统计」+「最终汇总」两段、无中间股票列表——属正常，**非格式错误**
- 每步运行数分钟（并发获取全市场数据），全程约 10–20 分钟
- 中间结果保存在各 skill 的 `data/` 目录，可单独查看
